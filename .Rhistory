pkgdown:::build_site_external()
knitr::kable(
data.table::fread("program.csv")
)
program <- data.table::fread("program.csv")
program[] <- lapply(program, function(x) {
if (is.character(x)) {
iconv(x, from = "", to = "UTF-8")
} else x
})
program[]
program <- data.table::fread("program.csv")
program[] <- lapply(program, function(x) {
if (is.character(x)) {
iconv(x, from = "", to = "UTF-8")
} else x
})
program[]
x <- data.table::fread("program.csv")
x[] <- lapply(x, function(col) {
if (is.character(col)) iconv(col, from = "", to = "UTF-8") else col
})
knitr::kable(x)
